import React from 'react'
import { IonSpinner } from '@ionic/react'
import './Loading.css'

const Loading = () => {
	return <IonSpinner className='spinner' color='primary' name='crescent' />
}

export default Loading
